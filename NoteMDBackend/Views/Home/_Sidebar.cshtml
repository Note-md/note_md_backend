@model IEnumerable<Course>


<nav id="sidebar" class="sidebar vh-100 overflow-auto">


    <ul class="nav flex-column gap-2 mt-5">
        @foreach (var course in Model)
        {
            var isActive = course.Id == ViewBag.SelectedCourseId ? "active" : "";

            <li class="nav-item">
                <a class="btn btn-outline-secondary border-0 text-white d-flex @isActive" asp-route-id="@course.Id" asp-action="Index" asp-controller="Home">
                    <span data-feather="home"></span>
                    @course.Name
                </a>
            </li>
        }
    </ul>
</nav>